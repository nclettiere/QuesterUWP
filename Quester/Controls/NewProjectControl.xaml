<UserControl
    x:Class="Quester.Controls.NewProjectControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Quester.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <StackPanel Width="320">
        <StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Top" Orientation="Vertical">
            <TextBlock TextAlignment="Center" HorizontalAlignment="Left" VerticalAlignment="Center">Project Name</TextBlock>
            <TextBox x:Name="ProjectNameTextBox" Text="{Binding ProjectName, Mode=TwoWay}" TextChanged="ProjectNameTextBox_TextChanged"  HorizontalAlignment="Stretch" PlaceholderText="Enter Name" Margin="0,10,0,0" AutomationProperties.Name="simple TextBox"/>
        </StackPanel>
        <StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Top" Orientation="Vertical" Margin="0,10,0,0">
            <TextBlock TextAlignment="Center" HorizontalAlignment="Left" VerticalAlignment="Center">Project Path</TextBlock>
            <TextBox HorizontalAlignment="Stretch" Margin="0,10,0,0" AutomationProperties.Name="simple TextBox"/>
        </StackPanel>
        <StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Top" Orientation="Vertical" Margin="0,10,0,0">
            <TextBlock TextAlignment="Center" HorizontalAlignment="Left" VerticalAlignment="Center">Short Description (Optional)</TextBlock>
            <RichEditBox x:Name="REBCustom"
                AutomationProperties.Name="editor with custom menu"
                Height="100" Margin="0,10,0,0"/>
        </StackPanel>

        <Button x:Name="CreateProjectButton" Click="CreateProjectButton_Click" Margin="0,15,0,0" HorizontalAlignment="Stretch" Style="{StaticResource PrimaryButton}">Create Project</Button>
    </StackPanel>
</UserControl>
